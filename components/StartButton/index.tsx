import styles from './index.module.css';

const StartButton = () => {

    return (
        <div className={styles.container}>
            <div>表单触发</div>
            <div>点击配置触发条件</div>
        </div>
    )
};

export default StartButton;
